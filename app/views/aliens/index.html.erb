<div class="container">
  <div class= "cards">
    <% @aliens.each do |alien| %>
      <div class="card">
        <h3><%= link_to alien.name, alien_path(alien.id) %></h3>
        <h3><%= alien.species %></h3>
        <h3><%= alien.planet %></h3>
    <% end %>
      </div>
  </div>
</div>

